name: Docker Image Build and Push

on:
  push:
    branches: [ actions-test ]
  pull_request:
    branches: [ main ]

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test docker
      uses: actions/checkout@v2
    - run: docker --version
    - run: docker-compose --version

  login:
    needs: [test]
    runs-on: ubuntu-latest
    steps:
    - name: Loggin into docker
      uses: actions/checkout@v2
    - run: echo "dockerBastard88!" | docker login -u "samdalvai@unibz.it" --password-stdin

  compose:
    needs: [test,login]
    runs-on: ubuntu-latest
    steps:
    - name: Compose docker images
      uses: actions/checkout@v2
    - run: docker-compose build